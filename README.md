# be-40
this is 40% implimentation of BR final year project

## Zero-knowledge face identity proof

See `zk/` for a Circom + zk-SNARK circuit that proves knowledge of a face embedding matching a registered commitment (without revealing the embedding). It includes a sample circuit and a walkthrough using `snarkjs`. 
